# Tasks

Use this directory for task specs and run state owned by this agent instance.

Suggested files:

- `inbox.md`
- `done.md`
- `recurring.md`

CLI:

```bash
neon task add "Call first provider smoke"
neon task list
```
