# Memory

Put your own project notes, decisions, summaries, and search indexes here.

This directory starts empty on purpose. Do not paste private Neon memory into another user's agent.

The installed local database lives in the state directory:

```bash
neon memory init
neon memory add --title "Decision" --body "..."
neon memory search "Decision"
```
